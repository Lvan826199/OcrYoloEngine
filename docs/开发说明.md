# 开发文档（持续更新）

> 本文件是**跨会话的开发主线索引**,每完成一个任务都要回来更新「进度表」。
> 新会话接手时,先读本文件即可掌握:项目定位、文档分布、当前进度、关键约定、如何继续。

- 最近更新:2026-06-10
- 当前阶段:全部 22 个基础任务 + 首版后增强 + 代码校验修复批次(v0.2.1)完成,154 个测试全绿(默认 146 + 冒烟 8)

---

## 1. 一句话定位

`OcrYoloEngine` 是面向**自动化测试**的视觉识别 HTTP 服务:其他自动化脚本截图发来,本服务用 OCR / YOLO / 模板匹配识别,返回坐标、文字、置信度,**只识别、不执行动作**。

---

## 2. 文档地图（权威来源,改动需同步)

文档已收敛为 `docs/` 下 7 个扁平的中文文件:

| 文档 | 作用 | 何时更新 |
|---|---|---|
| `docs/项目说明.md` | **项目说明 + 文档导航**:流程/核心技术/架构,顶部含文档导航表 | 整体定位或架构变化时 |
| `docs/快速开始.md` | **快速开始 + 小白手把手**:5 分钟跑通 + 从零开始 | 安装/启动方式变化时 |
| `docs/使用文档.md` | **使用指南**:API / CLI / 配置 / 错误码 | 接口、配置、错误码变化时 |
| `docs/部署文档.md` | **部署指南**:本地 / Docker(CPU/GPU)/ 调优 / 安全 | 部署方式变化时 |
| `docs/设计与决策.md` | **设计文档 + 架构决策记录(ADR)**:需求/范围/架构/数据模型/接口的权威来源 | 需求或架构变化、做出重要技术取舍时 |
| `docs/接口集成指南.md` | **平台对接用**:Python 客户端类 + 每个接口的完整调用代码 | 接口变化时 |
| `docs/开发说明.md`(本文件) | **开发主线索引 + 进度表 + 任务清单 + 约定速查** | 每完成一个任务 |
| `docs/bug修复日志.md` | **长期累积的 bug 修复记录**(倒序,现象/根因/修法/验证) | 每修一个 bug |
| `CLAUDE.md` | 给 Claude Code 的工作规则(语言/提交/文档同步/Bug 修复策略/报错必修等) | 规则变化时 |
| `MEMORY.md`(根目录) | 跨机共享的项目记忆(工作偏好/跨会话注意事项,CLAUDE.md 经 `@import` 自动加载) | 有新的重要记忆时 |
| `README.md` | 门面型说明 + 文档入口 | 对外信息变化时 |
| `CHANGELOG.md` | 变更记录(Keep a Changelog) | 每次有用户可见变更 |
| `plan/` 目录 | **计划类文档**:批次性修复/优化的方案(`YYYY-MM-DD-xxx.md`),一次性,不放日志 | 批次开工前 |

**规则**:以上文档互为引用;改了设计/架构必须回本文件更新进度与约定,改了规则必须同步进 CLAUDE.md 与 README.md。涉及架构取舍时在 `docs/设计与决策.md` 的「架构决策记录」一节新增一条 ADR。完整同步时机清单见 CLAUDE.md「文档引用与同步策略」与「Bug 修复策略」。

---

## 3. 实现计划（任务清单）/ 进度表（每完成一任务勾选并写完成日期)

> 逐任务的完整 TDD 步骤与全量代码已落到 `src/` 源码中,并完整保留在 git 历史(本次文档收敛前的提交)里,这里只保留任务清单。

状态:⬜ 未开始 / 🟡 进行中 / ✅ 已完成。

| # | 任务 | 状态 | 完成日期 |
|---|---|---|---|
| 0-1 | 工程脚手架与工具链 | ✅ | 2026-06-03 |
| 0-2 | 配置 settings.py | ✅ | 2026-06-03 |
| 0-3 | 错误契约 errors.py | ✅ | 2026-06-03 |
| 0-4 | 数据模型 schemas.py | ✅ | 2026-06-03 |
| 1-5 | 图片加载 loader.py | ✅ | 2026-06-03 |
| 1-6 | 预处理 pipeline.py | ✅ | 2026-06-03 |
| 2-7 | 识别器抽象 base.py | ✅ | 2026-06-03 |
| 2-8 | 模型注册表 registry.py | ✅ | 2026-06-03 |
| 2-9 | 模板库 store.py | ✅ | 2026-06-03 |
| 2-10 | 模板匹配识别器 template.py | ✅ | 2026-06-03 |
| 2-11 | OCR 识别器 ocr.py | ✅ | 2026-06-03 |
| 2-12 | YOLO 识别器 yolo.py | ✅ | 2026-06-03 |
| 3-13 | 工作池 executor.py | ✅ | 2026-06-03 |
| 4-14 | 结构化日志 logging.py | ✅ | 2026-06-03 |
| 5-15 | 配置加载 + DI + fakes | ✅ | 2026-06-03 |
| 5-16 | API Key 鉴权 auth.py | ✅ | 2026-06-03 |
| 5-17 | 识别管线 + FastAPI 路由 | ✅ | 2026-06-03 |
| 5-18 | HTTP 契约测试 | ✅ | 2026-06-03 |
| 6-19 | CLI cli.py | ✅ | 2026-06-03 |
| 7-20 | 隔离训练入口 training/ | ✅ | 2026-06-03 |
| 7-21 | configs + 资产目录 + Docker | ✅ | 2026-06-03 |
| 7-22 | 质量门禁(pre-commit + 门禁) | ✅ | 2026-06-03 |
| 8-23 | 模型热卸载/重载 HTTP 接口 | ✅ | 2026-06-04 |

### 首版后增强（已完成，2026-06-04）

- **测试改造为真实数据**:移除全部假识别器,默认套件改真实 `TemplateRecognizer` 端到端;新增真实模型冒烟(PaddleOCR / yolov8n / 模板 HTTP);写入「测试用真实数据(强制)」规则。
- **debug 标注图**:`debug=true` 返回画框 PNG(base64)。
- **模型热管理接口**:`/v1/models/{name}/unload|reload`。
- **golden 回归**:`tests/fixtures/` 提交确定性样例图 + 期望结果。
- **持续集成**:`scripts/check.sh`、`Makefile`、`.github/workflows/ci.yml`,Gitee Go 接入说明(见 §5.1)。
- **Prometheus `/metrics`**:各方法请求数与累计耗时。
- **Docker 改用 uv**:`uv sync --frozen` 按 `uv.lock` 安装,与开发/CI 同工具链、版本可复现。
- **可插拔结果缓存(默认关闭)**:`cache.py`(Null / LRU+TTL)、`OYE_RESULT_CACHE_SIZE/TTL_S`、请求级 `cache`(auto/refresh/off)、`from_cache` + `X-Cache` 头、`unload/reload` 失效、指标 `oye_cache_events_total`。`size=0` 时核心零开销。设计见 ADR 0003。
- **`/recognize` 可插拔多方法合并**:`merge.py`(`iou`/`merge_detections`)+ `RecognizeRequest.merge`(`none`/`priority`/`dedup`/`concat`,默认 `none`)+ `RecognizeResponse.merged`。`priority` 在 runner 短路(命中即停),`concat`/`dedup` 全跑后合并;与结果缓存兼容(键含 `merge`、`merged` 进缓存)。默认 `none` 零影响。设计见 ADR 0004。
- `.env.example` 示例配置。
- **资产配置 `.example` 模式**:`configs/models.yaml`、`templates.yaml` 不入库(gitignore),入库 `*.yaml.example` 模板(含 `yolov8n`/`demo_block` demo);`config_loader._resolve_config_path` 在实际文件不存在时自动回退读 `.example`,既开箱即用又规范(用户 `cp` 改自己的不冲突/不误提交)。
- **快速开始体验优化**:模板匹配(零额外依赖)替代 OCR 成首个体验步骤;示例改跨平台 Python 代码;新增 pip 备选与 Python 集成示例。
- **serve 端口自动轮询**:`ocr-yolo serve` 默认端口 8000 被占时自动尝试 8001~8009,找到空闲端口后提示并使用。

### 代码校验修复批次（已完成，2026-06-10，v0.2.1）

对 `src/` 全量代码校验后的集中修复,计划见 `plan/2026-06-10-代码校验修复计划.md`,逐条修复日志见 `docs/bug修复日志.md`:

- **P0 缺陷**:错误响应 request_id 恒为 `"-"`(改 async 中间件绑定)、upload 非法参数裸 500(转 422)。
- **P1 健壮性**:pin `paddleocr<3`;失败请求记入指标 `status="error"`;字节上限先于解码(防解压炸弹);模板匹配阈值下限 0.5 + 每尺度候选 top-200(防候选爆炸,ADR 0005);并发槽位改任务真正结束时归还(背压语义恢复,ADR 0006)。
- **P2 性能**:缓存键 hash 原始压缩字节;registry per-name 加载锁;模板匹配跳过模型锁真并发(`Recognizer.requires_serial_inference`);upload 免重复解码;模板灰度图缓存。
- **P3 清理**:删 `warmup` 死配置;API Key 常数时间比较;lifespan 关停线程池;JSON 日志加 UTC 时间戳;识别器统一继承 `Recognizer` 抽象(消 type: ignore);`serve` 端口耗尽明确报错。

### 运维增强批次（已完成，2026-06-10）

计划见 `plan/2026-06-10-运维增强批次.md`:

- **`X-Request-ID` 响应头**:中间件给所有响应(含错误)加头,与 body request_id 一致,便于调用方/网关关联。
- **JSON 结构化访问日志**:每请求一条(method/path/status/elapsed_ms/request_id),`JsonFormatter` 支持 `extra_fields` 合并。
- **夜间 smoke 流水线**:`.github/workflows/smoke.yml` 每日北京时间 03:30 + 手动触发,缓存 uv/PaddleOCR 模型/yolov8n 权重。
- **覆盖率门禁**:`check.sh` 加 `--cov-fail-under=85`(当前实测 88%)。
- 推送远端并打 `v0.2.1` tag。

### YOLO 训练教程批次（已完成，2026-06-10）

计划见 `plan/2026-06-10-yolo训练教程.md`,README 路线图「训练教程」项落地:

- **端到端教程**:`training/README.md` 重写(采集→标注→组织→训练→看懂结果→上线→迭代),`dataset.md` 并入后删除;每条命令 CPU 环境真实跑通,真实指标写入教程。
- **零标注 demo**:`training/make_demo_dataset.py` 合成游戏风格数据集(enemy/coin),5 分钟跑通全流程;3 个真实单测守护(结构/标签合法性/可复现)。
- **真实验证**:40 轮 CPU 训练 2.2 分钟、mAP50 两类均 0.995;自训权重登记进服务后 `/v1/detect` 检出坐标与真值偏差 <0.005。
- **train.py 修复**:无 GPU 机器默认参数必崩(见 `docs/bug修复日志.md`);训练完打印权重路径与上线三步提示。
- 训练产物加入 `.gitignore`(runs/、training/demo_dataset/)。

测试总量:默认 146 + 真实冒烟 8（共 154 个,全部通过)。`tests/fixtures/` 含真实游戏截图样例(SuperTuxKart,CC BY-SA 4.0,来源见该目录 README)做固定回归。仍待办(spec 列为 future):无。

### 需要手动测试的部分（自动化测试无法覆盖）

以下功能因需要交互式环境或外部资源,无法自动化测试,需人工验证:

1. **`ocr-yolo serve` 命令**：启动实际的 HTTP 服务器（交互进程,测试里用 TestClient 代替了真实启动）
2. **`ocr-yolo infer` 命令**：命令行推理真实图片（需要真实图片文件 + 配置好的模型/模板）
3. **`/v1/recognize/upload` 文件上传接口**：多部件上传格式的端到端验证
4. **Docker 镜像构建与运行**：CPU/GPU 两套镜像的构建和启动（需要 Docker 环境）
5. **GPU 部署**：显卡加速识别（需要 NVIDIA 显卡 + 驱动）

---

## 4. 关键约定速查（实现时务必遵守)

- **坐标契约**:识别器只吃预处理图,吐基于**输入图**坐标的 `RawDetection`;偏移回映射与归一化统一在 `preprocessing.finalize_detections`,识别器内部绝不自己做。
- **重依赖懒加载**:`torch`/`paddle` 只在对应识别器首次推理时 import,顶层与 `--help`/轻量命令不触发。
- **测试用真实数据(强制)**:不得用 mock/fake 替代识别核心;HTTP/契约用真实 `TemplateRecognizer` 端到端,OCR/YOLO 用真实模型放 `tests/smoke/`(`make smoke`)。识别器抽象 + 依赖注入仍保留(便于注入真实组件)。详见 CLAUDE.md「测试用真实数据」。
- **训练隔离**:`training/` 独立目录,`src/` 任何文件**不得** import,有测试 `test_training_isolation.py` 守门。
- **统一错误契约**:成功无目标 = 200 + 空 detections(不是错误);失败才 4xx/5xx + `error_code`。
- **每任务 TDD**:写失败测试 → 跑红 → 最小实现 → 跑绿 → 提交,每个大任务至少 3 个小步骤逐步实现逐步测试。
- **报错必修**:存在任何报错(测试失败、lint/type 报错)必须修复后才允许 commit/push。
- **提交规范**:Conventional Commits,描述用简体中文(见 CLAUDE.md)。

---

## 5. 环境与常用命令

直接用 `uv`:

```bash
uv sync --extra dev                 # 安装开发依赖
uv sync --extra dev --extra ocr --extra yolo   # 装全量(含 OCR/YOLO 重依赖)
uv run pytest                       # 跑测试(默认跳过 smoke)
uv run pytest -m smoke              # 真实模型冒烟测试(需全量依赖)
uv run ruff check src tests         # lint
uv run mypy                         # 类型检查
uv run pre-commit run --all-files   # 全量质量门禁
uv run ocr-yolo serve               # 启动服务
```

或用 `make`(本地与 CI 共用快捷入口):

```bash
make install        # = uv sync --extra dev
make install-all    # 装全量依赖
make lint / format / type / test / smoke
make check          # 全量门禁(= bash scripts/check.sh,CI 入口)
```

> 所有 git 与项目命令都在 `MyProject/OcrYoloEngine/` 内执行;父级 `MyProject/.claude/` 不属于本仓库。

---

## 5.1 持续集成（CI）

**门禁单一事实来源:`scripts/check.sh`**(lint + 格式 + 类型 + 测试,任一失败即非零退出)。本地、CI 都跑它,口径一致。

- **GitHub(镜像场景)**:`.github/workflows/ci.yml` 已配置,push/PR 到 `master` 自动跑 `scripts/check.sh`。
- **Gitee Go(主库)**:在 Gitee 仓库「流水线」里新建流水线,执行步骤填 `bash scripts/check.sh` 即可(与上面等价)。
- 默认 CI 只跑快测(真实图像端到端,无需重模型),`check.sh` 含覆盖率门禁(≥85%);真实 OCR/YOLO 冒烟较重,由 `.github/workflows/smoke.yml` 每日定时(北京时间 03:30)+ 手动触发跑,缓存 uv/PaddleOCR 模型/yolov8n 权重。

---

## 6. 后续会话如何接手

1. 读本文件 → 看「实现计划(任务清单)/ 进度表」确定下一个 ⬜/🟡 任务。
2. 按 TDD 步骤执行;已实现部分的完整代码见 `src/` 与 git 历史。
3. 跑测试 + lint + type,**全绿**后 commit(报错必修)。
4. 回本文件把该任务标 ✅ 并写完成日期;有用户可见变更时更新 `CHANGELOG.md`。
5. 涉及架构取舍时在 `docs/设计与决策.md` 的「架构决策记录」一节新增一条 ADR。
