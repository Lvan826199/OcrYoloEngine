# 运维增强批次计划（2026-06-10）

> 来源:v0.2.1 修复批次收尾后的"高价值、低成本"建议清单(经用户确认执行)。
> 执行约定:TDD(可测项先写失败测试)、报错必修、完成后按 CLAUDE.md「文档引用与同步策略」同步文档。

## 任务清单

| # | 任务 | 内容 | 验证 |
|---|------|------|------|
| 0 | 推送远端 + 打 tag | 推送本地 master 至 origin(Gitee);在版本号升级提交上打 `v0.2.1` 标签并推送 | 远端可见全部提交与 tag |
| 1 | `X-Request-ID` 响应头 | request_id 中间件统一给**所有**响应(成功与错误)加 `X-Request-ID` 头,与 body 一致 | 契约测试:成功/错误响应头与 body request_id 相同 |
| 2 | JSON 访问日志 | 同一中间件记结构化访问日志(method/path/status/elapsed_ms/request_id);`JsonFormatter` 支持 `extra_fields` 合并 | 单测:formatter 合并 extra;契约测试:请求产生 access 记录 |
| 3 | 夜间 smoke 流水线 | 新增 `.github/workflows/smoke.yml`:cron 夜间 + 手动触发;装全量 extras,缓存 uv/~/.paddleocr/yolov8n.pt;跑 `make smoke` | workflow 文件语法正确;本地 `make smoke` 全绿 |
| 4 | 覆盖率门禁 | `scripts/check.sh` 的 pytest 加 `--cov=ocr_yolo_engine --cov-fail-under=<锚定值>`(取当前实测覆盖率向下取整) | check.sh 全绿;故意降低阈值外的覆盖会失败(逻辑自证) |

## 明确不在本批的项(场景驱动,等瓶颈出现)

- OCR 引擎池(并发吞吐)、结果缓存默认开启、大图模板匹配金字塔、async 路由改造、推理进程池。

## 收尾

1. 文档同步:`CHANGELOG.md`(未发布)、`docs/使用文档.md`(X-Request-ID 头)、`docs/开发说明.md`(批次记录)、`docs/部署文档.md`(如涉运维说明)。
2. 本批为增强非缺陷,不记 `docs/bug修复日志.md`。
3. 完成后推送。
