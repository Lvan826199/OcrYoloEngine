# 自定义 YOLO 模型训练端到端教程——计划（2026-06-10）

> 来源:README 路线图「规划中」第一项,用户确认优先做。
> 现状:`training/` 只有骨架(train.py + 格式说明),从未真实跑通;`train.py` 默认 `device="0"` 在无 GPU 机器必崩(顺手修,记修复日志)。
> 原则:教程的每一步都要**真实跑通一遍**,真实输出写进教程;训练隔离规则不变(src 不得 import training)。

## 任务清单

| # | 任务 | 内容 | 验证 |
|---|------|------|------|
| 1 | demo 数据集生成器 | `training/make_demo_dataset.py`:合成"游戏风格"两类目标(enemy 红方块 / coin 黄圆)数据集,生成 images/{train,val} + YOLO 标签 + data.yaml。让教程**零标注成本**端到端体验 | 真实单测:目录结构/标签格式/坐标 0~1/类别集合(tests/unit) |
| 2 | train.py 修复与易用性 | device 默认 `"0"` → 默认自动(无 GPU 回落 CPU);训练完打印 best.pt 路径与"登记到服务"下一步提示 | 真实训练跑通(任务 3);修复记 docs/bug修复日志.md |
| 3 | 真实端到端验证 | 用 demo 数据集真实训练 yolov8n(CPU、小 epochs、imgsz 320)→ best.pt → 登记 `configs/models.yaml`(本地不入库)→ 服务 `/v1/detect` 对验证图检出目标 | 检出类别/坐标与生成时的真值一致(带容差);真实输出片段写入教程 |
| 4 | 端到端教程 | 重写 `training/README.md`:①5 分钟 demo 快速通道 ②真实业务流程(截图采集→标注工具与 YOLO 格式→数据组织→训练参数→看懂训练结果→权重放置→服务登记→调用验证→迭代建议);合并 `dataset.md` 后删除 | 教程每条命令均为验证过的真实命令 |
| 5 | 文档同步 | README 路线图勾选;CHANGELOG;开发说明批次记录;`.gitignore` 加训练产物(training/runs/、training/demo_dataset/) | 全绿提交推送 |

## 范围外

- GPU 训练实测(无显卡环境,教程给参数说明并标注未实测)。
- 标注工具的安装教学(链接 + 格式要求即可,不重复官方文档)。
